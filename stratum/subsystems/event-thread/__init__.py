# Event Thread — Cross-day story tracking + cross-temporal linkage.
#
# STATUS: ACTIVE (v5.0)
# Python:
#   event_thread.py   — deterministic core (lifecycle, matching, watch queries)
#   cross_temporal.py — vertical linkage across time scales (daily→weekly→monthly→quarterly→yearly)
#   contracts.py      — cross-temporal dataclasses
# Agent: skills/event-thread-engine/SKILL.md — LLM-driven semantic matching
